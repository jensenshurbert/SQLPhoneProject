
#include "PhoneEntry.h"

PhoneEntry::PhoneEntry() {
}

PhoneEntry::PhoneEntry(string f, string l, string p, string t, string r) {
    first = f;
    last = l;
    phone = p;
    type = t;
    recnum = r;
}
